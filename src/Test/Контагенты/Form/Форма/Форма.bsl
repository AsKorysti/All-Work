
&НаСервере
Процедура окНаСервере()
	ВыборкаДокументов = Документы.ОказаниеУслуги.Выбрать(ДатаН, ДатаК); 
	
	Пока  ВыборкаДокументов.Следующий() Цикл 

		
		Если ВыборкаДокументов.Клиент = Контагент Тогда 
			Сообщить(ВыборкаДокументов.Дата);	
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура ок(Команда)
	окНаСервере();
КонецПроцедуры
